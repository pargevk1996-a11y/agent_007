"""Plan generation, validation and revision.

Produces a typed plan the user can inspect, edit or reject before execution spends
anything on tools. Plan revisions are immutable: editing a plan creates a new one.
"""
