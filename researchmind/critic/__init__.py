"""Claim-versus-source verification.

Runs on an evidence-only context: the critic sees the claim and the source excerpts,
never the executor's reasoning. That isolation is the primary bias mitigation.
"""
