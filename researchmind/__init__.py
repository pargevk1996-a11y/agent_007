"""researchmind: turns an open-ended research question into a cited, verifiable report.

The package layout encodes a dependency layering that is documented in README.md and
enforced in CI. Nothing is re-exported from this module on purpose: every import names
the module it comes from, so boundaries stay visible at the call site.
"""
